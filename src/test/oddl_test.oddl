CREATE CLASS stu_ (
	name CHAR(10),
	gender CHAR(1),
	age INT,
	stuid INT,
	isgraduate BOOLEAN,
	testfloat FLOAT
);

CREATE SELECTDEPUTY graduate
SELECT name AS name,
       gender AS gender,
       age AS age
FROM stu_
WHERE isgraduate = TRUE;

DROP CLASS stu_;
